# Engine strict mode - require exact node version
engine-strict=true

# Prevent auto-saving versions
save=false
save-exact=true

# Use pnpm specifically
package-manager=pnpm@10.28.0

# Prevent version upgrades
prefer-dedupe=false